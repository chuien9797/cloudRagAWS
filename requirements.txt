# API FRAMEWORK
fastapi==0.115.0
uvicorn[standard]==0.30.6


# RAG / NLP CORE
# NOTE: torch (CPU-only) is installed separately in the Dockerfile BEFORE
# this file is processed, so pip will not pull the 2 GB CUDA wheel here.
sentence-transformers==3.0.1
chromadb==0.5.5


# TEXT PROCESSING
nltk==3.9.1
python-docx
pymupdf
rank-bm25
httpx

# AWS
boto3==1.34.131


# DATABASE
sqlalchemy==2.0.31
asyncpg==0.29.0

# NUMPY (pinned to satisfy sentence-transformers + chromadb)
numpy==1.26.4


# STABILITY / VALIDATION
pydantic==2.8.2
python-multipart==0.0.9